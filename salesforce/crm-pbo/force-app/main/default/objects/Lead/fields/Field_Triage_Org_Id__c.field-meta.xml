<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Field_Triage_Org_Id__c</fullName>
    <label>Field Triage Org ID</label>
    <type>Text</type>
    <length>18</length>
    <!-- externalId + unique is what makes the upsert possible: the app PATCHes
         /sobjects/Lead/Field_Triage_Org_Id__c/{orgId} and Salesforce decides
         insert-vs-update itself. Without both flags that endpoint 400s, and a
         repeat scan would create a duplicate Lead instead of refreshing one. -->
    <externalId>true</externalId>
    <unique>true</unique>
    <required>false</required>
    <caseSensitive>false</caseSensitive>
    <trackHistory>false</trackHistory>
    <description>The Salesforce org ID (18 char) that ran the Field Triage scan. Written by the triage.datajungle.io lead magnet and used as the upsert key, so one org is one Lead however many times they re-scan.</description>
    <inlineHelpText>The org this lead scanned with Field Triage.</inlineHelpText>
</CustomField>
